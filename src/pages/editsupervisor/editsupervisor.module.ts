import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { EditsupervisorPage } from './editsupervisor';

@NgModule({
  declarations: [
    EditsupervisorPage,
  ],
  imports: [
    IonicPageModule.forChild(EditsupervisorPage),
  ],
})
export class EditsupervisorPageModule {}
